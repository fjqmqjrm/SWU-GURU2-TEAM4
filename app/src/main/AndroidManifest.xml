<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GURU2_Team4"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RegisterActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".QandAActivity" />
        <activity android:name=".QViewDetailsActivity" />
        <activity android:name=".AViewDetailsActivity" />
        <activity android:name=".RequestActivity" />
        <activity android:name=".MyPageActivity" />
        <activity android:name=".PartPageActivity" />
        <activity android:name=".PartPageActivity2" />
        <activity android:name=".PartPageActivity3" />
        <activity android:name=".PartPageActivity4" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />
    </application>

</manifest>